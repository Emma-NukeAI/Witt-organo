# HANDOFF BACK — 2026-08-23 (para la siguiente conversación en witt-organogenesis)

**Estado en una línea:** el backlog backend solo-atacable está **VACIADO** (ADR-0043…0073), producción
viva en `witt-ai.com.mx` post-Redeploy 2026-08-23, todo verificado en vivo (prueba 14/14 + ADR-0065
operativa + primera calificación M5). Los repos back y front están limpios y pusheados. Lo que resta
tiene dueño y ninguno es "código backend que se pueda atacar en solo".

---

## 1. Qué es el sistema hoy (contrato 1.6)

Una corrida (`runs.execute_run`, `rag_index/query_service/runs.py`) = plan opcional v3 → retrieve DI →
**pass1 DI-only** → **elicitación dedicada de confianza** (`CONF_TOOL`, ADR-0065: `confidence.source:
stated-second-elicitation`, in-line persistido como cross-check) → fallback por confianza o estructura
(τ=0.5, `fallback.trigger` declarado) → Ruta B (europepmc + zfin + pubmed, Layer 0) → pass2 + delta →
gate determinista → **panel 4 jueces** (Opus+Sonnet+Haiku+gpt-4o, worst-of-N, `APPROVE_DECLINE` para
declinación honesta) → **ciclo de revisión acotado** post-REVISE (ADR-0067: cap DURO=1, nada se borra)
→ registro congelado en Postgres → cierre explícito → **calificaciones M5** append-only + `/calibration`
con poder declarado (ADR-0064) → **PDF de servidor** (`GET /runs/{id}/record.pdf`, ADR-0073).

Puertas nuevas de la última tanda: `GET /consulta-sistema` (determinista, ADR-0070) ·
`GET /rack/node/{id}` (browse del grafo, dos backends con `browse_mode`, ADR-0071) ·
`evaluation/run_held_out_v2.py` (evals SOBRE el run model, BD local forzada, ADR-0072).

## 2. Verificado EN VIVO (2026-08-23, no asumir — ya está hecho)

- **Redeploy de Emmanuel + prueba de 2 centavos: 14/14 PASS** (ANTHROPIC_API_KEY de prod funciona;
  plan META → `store-consultation`; plan v3 con `data_landscape`; consulta-sistema; rack/node modo
  graph; PDF retrocompatible sobre registro 1.1 real). Detalle: addendum de
  `witt-ui-lab/HANDOFF-2026-08-22.md`.
- **ADR-0065 verificación operativa CUMPLIDA**: corrida real `4d046355…` (la pregunta wt1a de la
  corrida #1 re-corrida) → `stated-second-elicitation` en ambas pasadas (0.05→0.86, |Δ| in-line ≤0.05,
  cero regex), fallback confidence, ZFIN+EPMC, panel 4 válidos → `AUDIT_APPROVED`, $0.25 [E]. El arco
  corrida#1-sin-evidencia → ingesta CORPUS-2026-0009 → aprobada quedó CERRADO. gap_flag honesto de la
  corrida: el claim se sostuvo en el fetch ZFIN vivo, no en la DI local (la ingesta F4-gateada es lo
  que subirá pass1).
- **Primera calificación M5 de producción**: Emmanuel cerró `4d046355` y la calificó vía API (la UI no
  existe aún — pendiente FRONT): input 3 / output 2, `m5-cierre`, `is_author: true`, perfil dev.
  `/calibration` vivo respondió **n_scored=1, "case capture", sufficient=false** con su nota de poder —
  exactamente el diseño ADR-0064 (jamás un ECE ciego). Consenso de esa corrida: 1/5, faltan los 4 médicos.
- Producción: **5 corridas** (2× wt1a: `a361f566` histórica + `4d046355` nueva; 2× señal upstream; 1
  failed), store **113** / 34 docs / 106 entities, index ONLINE.

## 3. Pendientes por dueño (nada backend-solo)

**Emmanuel (humano, gates):**
- **Muestra F4** (~20 min): `rag_index/curation/quarantine/zfin_sweep_20260822T232036Z/validation_sample.csv`
  → destraba la ingesta ZFIN gateada (los 16 statements de wt1a incluidos) y la 2ª etapa EPMC.
- **Ratings de los médicos** (marcelo/martin/natalia/sharon, `m5-consenso`) → volumen para ECE real
  (n≥10) y umbrales del Tapón 5.
- **Cuenta Resend** → destraba M9.
- **Decidir baseline held-out v2 completo** (30 preguntas, ~USD 6–9, `evaluation/run_held_out_v2.py`).
- **Sus "2 ajustes que requieren análisis"** (mencionados 2026-08-22) — NUNCA los enunció; preguntarle.

**Sesiones futuras de back (cuando haya insumos):**
- Cron + umbrales del Tapón 5 (el instrumento ya existe: held-out v2 + EVAL_DESIGN.md).
- 2ª etapa de curación: agentes-lectores EPMC sobre la plantilla ADR-0068 (tras F4).
- M9 notificaciones (tras Resend).

**FRONT (carril UI de Emmanuel):** ver `witt-webapp/HANDOFF-UI-2026-08-23.md` — encabezado: **la M5 UI
de calificaciones no existe** (Emmanuel cerró una corrida y no le apareció nada para calificar; el
backend está completo y la cola `GET /ratings/pending` le tiene 4 corridas esperando).

## 4. Cómo verificar rápido (todo offline salvo lo marcado)

Python de trabajo: `C:/Users/Emmanuel/dev/.venvs/witt-query-service/Scripts/python.exe` (JAMÁS tocar el
`.venv` del MCP, ADR-0039). Gates y sus valores verdes actuales:

| Gate | Esperado |
|---|---|
| `rag_index/query_service/smoke_run_pipeline.py` | 118/118 |
| `rag_index/query_service/smoke_query_service.py` | 45/45 |
| `rag_index/query_service/smoke_ratings_calibration.py` | 29/29 |
| `rag_index/query_service/smoke_precedent.py` | 15/15 |
| `rag_index/ingest_service/smoke_ingest_gate.py` | 15/15 |
| `evaluation/smoke_run_held_out_v2.py` | 12/12 |
| `rag_index/curation/smoke_zfin_sweep.py` | 12/12 |
| `substrate_calibration/tools/doc_coherence_check.py` | 7/7 · 0 FAIL |

Contra producción (gasta ~centavos, requiere sesión): patrón de
`scratchpad/prueba_2_centavos.py` de la sesión 08-23 — login → token a
`.secrets/session_token.txt` (expira 72 h, jamás por chat) → GET/POST con bearer.

## 5. Higiene y reglas activas

- Secretos: token de sesión y credenciales SOLO en `.secrets/` (gitignored). Credenciales dev rotadas
  2026-08-22 (`.secrets/dev_creds_rotated_20260822.txt`). API keys jamás a git.
- Evals: BD **forzada** a `evaluation/eval_runs.db` (run_held_out_v2 la impone) — jamás capturan la BD
  de producción ni entran al precedente.
- DI: **113 registros, cero mutación** en toda la línea 08-19→23. Toda mutación = gate humano (§7).
- failed/cancelled: estatus terminales aparte — se pueden calificar (registro) pero JAMÁS entran a
  precedente/DI/ECE (decisión Emmanuel 2026-08-22, en ADR-0064).
- Push: solo a `origin` = `Emma-NukeAI/Witt-organo` (privado).

## 6. Dónde está la historia

- `CLAUDE.md` §12 (la bitácora densa; entrada 2026-08-22 items 1–9 = esta línea).
- `docs/decisions/README.md` — índice ADR 0001–0073.
- `witt-ui-lab/HANDOFF-2026-08-22.md` — el hilo cross-carril completo (con addendum 08-22/23).
- `witt-webapp/HANDOFF-UI-2026-08-23.md` — el handoff del carril UI (gemelo de este).
- Memoria de agente: `webapp-backend-pre-ui-20260809.md` (sincronizada a 08-23).
